[project]
name = "aiagent"
version = "0.1.0"
description = "throttled and limited tool use with openai and openrouter"
readme = "README.md"
requires-python = ">=3.13.2"
dependencies = [
    "openai==2.44.0",
    "python-dotenv==1.1.0",
]
